let fruits=["Mango","Banana","Litchi","Apple"];
let fruits1=document.getElementById("array") //get the id of h1 tag in html document
let f=fruits1.textContent=fruits;

//array method
let len= fruits.length;
fruits1.textContent=len + f;
